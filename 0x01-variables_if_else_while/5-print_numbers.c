#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Entry point of the program
 *function serves as the starting point of the program
 *Return: always 0 on successful execution
 */

int main(void)
{
	int m;

	for (m = 0; m <= 9; m++)
	{
		printf("%d\n", m);
	}
	return (0);
}
